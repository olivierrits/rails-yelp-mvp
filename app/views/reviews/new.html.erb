<h1>Add a Review for <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.select :rating, Review::STARS %>
  <%= f.submit "add review" %>
<% end %>

